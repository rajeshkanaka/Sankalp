# Partial offline integration evidence — 2026-09-06

Run `offline-fallback-check`, source e1749a0 plus coordinator working changes later checkpointed on offline-recovery (dirty working tree). Node24.20.0, Next16.3.4, Playwright1.63.0, local PostgreSQL17.6 slot1/migrations001–008. Synthetic fixture clock2026-09-05T00:45Z; synthetic accounts only. Public-shell registration is deliberately denied and one reflection request is deliberately aborted in the fallback scenario; real authentication, creation, retry persistence and signout use the actual app/local services.

`UI_RUN_ID=offline-fallback-check fnm exec --using 24.20.0 npm run test:ui -- --grep 'online-only editors|offline replay HTTP|metadata'`: **FAILED overall;4PASSED,3FAILED**. All3 fallback preservation scenarios and Chromium real HTTP boundary scenario passed. All3 delayed-metadata scripts failed (route cleanup/interception harness defects); fixes and reruns are pending. See safe summary.json for exact test outcomes. Screenshots show actual unsaved-note signout choices, not a completed offline integration. Chromium screenshot visually inspected by coordinator; functional assertions verify retained exact input and later canonical save in all3 engines. No milestone/task completion inferred.
