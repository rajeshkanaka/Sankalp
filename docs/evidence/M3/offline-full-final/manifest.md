# Offline full regression — 2026-09-06

**98/98 Playwright cases passed** across Chromium, WebKit and Firefox in16.5minutes, with retries disabled locally. Command: `UI_RUN_ID=offline-full-final fnm exec --using 24.20.0 npm run test:ui`. Production build/source was5c8962f; that build already passed formatting, lint, types,215unit/84database tests and smoke. The GitHub Autofix commitce56222 (closing the second MessagePort) arrived during this run and was preserved by fast-forward; this result does not pretend to have tested the changed build.

The suite exercises real captured-email authentication, PostgreSQL persistence, personal schedules, numeric targets, progress/calendar, historical corrections, Unicode reflections, actual network cutoff/reload/replay, two-device conflict review, storage failure, stale identity, sign-out races and public-cache restrictions. Online-only page-route response simulations are explicitly named in their tests; browser storage harnesses use real IndexedDB/Web Locks with simulated backend transport. No actual phone notification delivery or VoiceOver speech pass is claimed.

The safe result is `summary.json`. Its legacy `sourceSha` was sampled at run end after the fast-forward; the added `testedBuildSha` explicitly identifies the unchanged5c8962f production build. The reporter now captures the run-start revision, with a regression proving mid-run context changes cannot relabel it. Synthetic screenshots are under the matching M1/M2/M3 `offline-full-final` directories. Full local log: `artifacts/offline-full-final.log` (ignored). One expected server stream-close diagnostic occurred while the outage/conflict fixture interrupted network traffic; functional assertions and the suite passed. This is not evidence of real internet outages or physical devices.

The prior77PASS/9FAIL and14PASS/1FAIL snapshots remain historical evidence; their failures are superseded for build5c8962f by this complete fresh run. The final port-cleanup build requires fresh checks and exact-head CI before normal PR5 merge. Independent bounded source reviews found no remaining actionable high-impact defect; full app completion remains governed by TASKS and its native/external gates.

Final applicationce56222 freshly passed `npm run verify`:215unit/84database tests, formatting/lint/types, public-shell/Next build and Chromium smoke. Reporter correction separately passed5focused tests, lint/types/format. Exact-head hosted CI and targeted final service-worker checks remain recorded in the following checkpoint.
