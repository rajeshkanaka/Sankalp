# SK-008 coordinator handoff

This is the current coordinator entry point; task status remains in [TASKS](../TASKS.md). The accepted baseline and worktree map live on main in [PROJECT_PROGRESS](../PROJECT_PROGRESS.md) and [BRANCHES](../BRANCHES.md).

## Reconciled resume — 2026-09-06

The user resumed work and authorized reviewed PRs/merges to make main the durable starting point. The overnight integrated app at `ef6e0a9` passed hosted CI33995477654 (verify, full UI and audits); fresh local resume verification also passed115unit/83DB/build/smoke. Offline core/UI remain unfinished on their named branches. Prepared source at `ef6e0a9` is not evidence of working offline behavior.

Read [SK-008-break](SK-008-break.md) for the preserved historical break and the branch-local reports using `git show task/SK-008-core:docs/handoffs/SK-008-core.md` and the corresponding UI command. Those reports describe WIP; their old shared tracking documents never supersede main.

Exact order after main consolidation:

Concrete core defects, interface refinement and required regressions are recorded in [the resume review](SK-008-resume-review.md); this is the first worker brief after assignment.

1. Coordinator creates the recorded main-based feature/worker assignments, carries the preserved core/UI commits without old tracking documents, and restores only the required coordinator-owned public shell source from `ef6e0a9`.
2. Core: regression/fix for resolving stale comparisons after newer canonical updates or independent session/reflection acknowledgments; reconcile actual HTTP `ACCOUNT_CHANGED` with transport handling; finish storage-denial/quota/transaction-abort and500-operation tests; repair harness globals and use the existing macOS Firefox `MOZ_APP_DATA` convention. No root runtime access.
3. UI: finish missing CSS and static checks against the approved core interface; verify account invalidation, raw-draft compare-and-swap and unsaved-data controls. Preserve in-memory input on storage failure.
4. Coordinator: complete the public shell entry point, exact asset/cache validation, build/CSP/proxy exclusions and account/session/signout mounting. The old offline builder references a missing entry point and is intentionally absent from accepted main until implemented and tested.
5. Launch the actual app and run real offline-edit/reload/reconnect/retry/conflict/account/storage tests. Keep mocks limited to explicitly labeled harness transport. Verify UI, database boundaries and full regression before proposing the next accepted PR.

No app offline pass, native B06 pass, phone notification evidence or deployed service is claimed. Keep noncritical preparation moving while genuine external blockers remain in TASKS.


## Active recovered source

Main accepted PR3/eac5cdc and tracking d7a1096. Feature c4b0666 carries the preserved core WIP plus the coordinator's token-bound full-sequence resolution contract; UI148ca78 carries its original source. These commits are recovery/contract checkpoints, not passing implementations. Current workers and scopes are in BRANCHES/TASKS. Core and UI fix their reviewed defects concurrently; coordinator prepares actual public shell and app/account integration.

UI review found failed draft persistence resetting dirty state, comparison displaying proposed rather than server values, hidden deletion of later queued intents, stale account/generation rendering, stuck signout/shared-device recovery, and absent-reflection/blocked-queue labeling defects. Fix with actual input-preservation, account lifecycle and full reviewed queue tests. The corrected replacement contract retains raw draft separately; UI must clearly identify unsent raw input and display the full affected operation sequence before resolution.

Nonblocking PR3 diagnostics followups belong to coordinator's existing HTTP/proxy scope: explicit requiredEnv guards before SSR client construction and a specific malformed Idempotency-Key error inside the authenticated route operation. Keep authentication/origin/body validation ordering and no-write failure behavior.

## Integrated offline checkpoint — 2026-09-06, midday

Root branch `rajesh_kanaka/offline-recovery` at `/Users/rajesh/sankalpa`, slot1. Accepted main remains `d7a1096` after PR3/eac5cdc; PR4 contains PR-only checkpoint policy and a separately reviewed metadata hydration correction. Current feature source is not accepted main. No task promoted to DONE.

Core correctness integrated as `9d4ca21` and real-browser storage regressions as `b5699de`; UI checkpoint `dae197e` is under further lifecycle review. Coordinator now built the actual public shell (Vite browser-only bundle, explicit immutable asset list, worker fallback with no private HTML/API/RSC caching), mounted account/session/signout controls and added online-only editor checkpoints. Invalid creation operation headers now return a specific safe422 diagnostic; proxy configuration uses validated required environment settings.

Observed verification on the coordinator working tree: formatting of touched source, full lint/typecheck and production public-shell/Next build PASS. Focused core+cache-policy unit run9PASS. First six-case actual UI run: Chromium smoke and public offline shell PASS; WebKit controlled-checkbox timing and Firefox provider readiness FAILED, plus both engines' emulated-offline navigation FAILED. Follow-up run `offline-fallback-check`: online-only draft/signout cancellation and exact failed-note retry PASS in all3 engines; real HTTP account/ownership/idempotency boundaries PASS in Chromium. All3 metadata-delay tests FAILED because routing did not wait for held handlers and WebKit's active worker obscured page routing. Hydration test owner is correcting the harness; these failures remain failures until a fresh run succeeds.

Evidence: ignored `artifacts/offline-{first,fallback}-*.log`, `artifacts/ui/summary.json` and failure diagnostics; synthetic screenshots in `docs/evidence/M1/offline-integration-first` and `docs/evidence/M3/offline-fallback-check`. Partial screenshots are not a passed milestone. Independent review and actual isolated transport tests found Playwright's WebKit offline emulation blocks even cached documents; Firefox may leave worker fetches online. Actual server cutoff served the real worker fallback in all3 engines. The core owner is producing a guarded test-only proxy/helper to verify actual network failure deterministically; browser-airplane simulation and physical-device evidence remain distinct.

Active unfinished work: UI owner fixes secure mounted-state withholding on storage read failures, cross-tab draft cleanup CAS, canonical history notification, verified local purge on signout, raw invalid numeric text and initial checkbox/provider timing. Coordinator verifies fallback pending-request, mood/time account-action coverage and integrates the test-only network helper. Hydration owner fixes delayed-script regression and updates PR4 through normal push/CI. No force push or direct-main bypass is permitted.

Exact next step: integrate the next reviewed UI and hydration-test commits, switch the test server to the deterministic proxy, rebuild, then rerun focused actual offline/fallback/revision/smoke workflows before full regression. Do not resume from partial screenshot success or redo accepted M1–M3 work. Runtime restart remains `fnm use 24.20.0`, `npm run db:start`, `npm run build`, `npm run test:smoke`; demo `npm run dev:demo -- --profile M3` on localhost3001. Assume no app/test process survives; local database slot1 is running, slot0 preserved untouched.
