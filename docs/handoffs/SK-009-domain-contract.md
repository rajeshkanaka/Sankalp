# SK-009 pure reminder contract — frozen coordinator scope

2026-09-06. This is isolated preparation under D12 while SK-008 regression and the native capability gate remain open; no M3/M4 completion is implied. No stack change. TASKS remains the status authority. SQL, route and worker contracts will be frozen separately before SK-010/011 assignment.

Owner /root/ci_triage in `/Users/rajesh/sankalpa-worktrees/SK-009-domain`, branch `rajesh_kanaka/reminder-domain`, base accepted main544f3ef. Permitted files: new `src/domain/reminders.ts`, `tests/unit/reminders.test.ts`, and `docs/handoffs/SK-009-domain.md`. No shared validation/contracts, dependencies, configuration, database/runtime, worker or UI changes. Uses existing Temporal0.5.1 and Zod4.5.4. Reuses existing ReminderPreferences and SessionRecord types. The local dependency symlink appears untracked under the main-based ignore rule; never stage it. Explicitly stage only the three owned files. Coordinator integrates only reviewed owned commits after current feature isolation is preserved.

Export strict `reminderPreferencesSchema`: disabled by default in callers; one to eight unique integer offsets in−1440..0 when enabled, zero to eight when disabled; no coercion. Quiet hours null or strict minute HH:mm start/end; equal bounds invalid. Detailed payload choice is boolean. Reject unknown fields. Existing journey schema adopts this one schema when reminder routes are integrated.

Export `quietHoursContain(instant, timeZone, quietHours)`: half-open[start,end) wall-time membership in the session's snapshotted IANA timezone, wrapping midnight. The intended instant's actual civil date/DST offset matters; no inferred practice-date subtraction and no shifting reminders. Null means false.

Export `planSessionReminders(session,prefs,now)`: session is the Pick of SessionRecord opensAt,closesAt,timeZone,confirmed,supersededAt. Return one item per configured offset in chronological order with offsetMinutes,scheduledAt,expiresAt and nullable suppressionReason. Reasons: disabled,completed,superseded,past,quiet_hours. Eligibility requires incomplete/nonsuperseded/enabled and intended instant strictly after now and outside quiet hours. expiresAt=min(intended+5minutes,closesAt). Calculations use canonical instants, not browser-local dates. No jobs, sends, IO or mutable clocks. Callers must separately enforce journey/account/device eligibility and transactional generations.

Export `planSnooze(session,prefs,now)`: discriminated eligible result with scheduledAt=now+10minutes and expiresAt=min(target+5minutes,closesAt), or reason disabled,completed,superseded,not_open,window_closed,deadline,quiet_hours. Requires opensAt<=now<closesAt and target strictly before close/outside quiet hours. Active-device and operation idempotency checks remain server responsibilities. Caller atomically replaces pending reminders in[now,target]; this pure module does not mutate records.

Tests: exact midnight reminder dates A02; offsets duplicates/range/count/unknown/type rejection; disabled suggestions; quiet wrap/exclusive boundaries/equal rejection; DST spring/fall instant membership; exact-now skipped; five-minute/deadline expiry; confirmed/superseded suppression; snooze opening/close/target boundary and quiet cases. Table-driven Vitest using real Temporal arithmetic, no external transports. Record actual focused and full unit/static results; no application UI/database/device pass is implied.
