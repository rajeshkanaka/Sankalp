# SK-003 personalized setup UI handoff

Owner: `/root/m1_domain`

Branch/worktree: `task/SK-003-setup` at `/Users/rajesh/sankalpa-worktrees/SK-003-setup`

Base: `36c4f95a05a2fc3d0f1549842c4c871972aaff85`

Implementation commits:

- `4aed82d` — `SK-003: add personalized setup and numeric practice UI`
- `bcc12df` — `SK-003: verify numeric save retry preserves input`
- `c63ec35` — `SK-003: preserve failed practice saves`

## Implemented

- The setup route remains blank by default and retains every M1 form label and action selector. An explicit `Use 21-night example` action fills editable Kunjika/Bhairav values, midnight previous-evening attribution, Asia/Kolkata, 21 occurrences, a four-hour window and disabled `[-120,-30,-5,0]` reminder suggestions. Template selection resets timezone confirmation and creates no shared template record.
- Practices support completion checkboxes, repetition targets and minute targets with D04 UI bounds. Schedule setup supports occurrence and calendar-day duration, daily or arbitrary ISO weekdays, explicit civil or previous-evening attribution, local time, wall-clock window and confirmed IANA timezone.
- The review screen renders the server-returned practice targets, actual total, duration semantics, selected weekdays, first/final opening instants, first/final stable practice dates, timezone and attribution. It identifies reminder suggestions as off/no delivery, shows first-session suggestions and past/skipped state, and renders DST/overlap warnings accessibly.
- Practice recording retains checkbox auto-save while numeric practices use local string drafts and explicit per-item Save actions. Mutations contain only the changed practice ID. Invalid or failed saves retain the user's entry and exact mutation envelope. After a request fails, all practice controls remain disabled until that exact save succeeds or the conflict reload path is chosen, so another practice cannot replace the pending retry. A saved positive value below target is visibly Partial and cannot be confirmed, while a saved target value permits the existing separate confirmation action.
- Accessible names include their visible labels plus practice context. Numeric Save uses the stable visible text `Save value`; its longer accessible name and a 120-character unbroken practice label wrap within the 320 CSS px layout.
- `tests/ui/m2-schedule.spec.ts` performs one real captured-email sign-in per browser account, changes only the guarded allocated UI clock, and restores the M1 clock in `finally`. It covers blank A23 behavior, explicit A01 template dates/reminder suggestions, zero-weekday input preservation, A24 Monday/Thursday end date and numeric target, A25 Tuesday-only calendar span, a failed checkbox blocking another practice until exact retry, failed numeric save/retry, Partial gating, accessible-name assertions, a 120-character label at 320 CSS px, axe checks, reflow and `weekday-preview.png`.

## Verification

Run with Node/npm selected by `fnm exec --using 24.20.0`. The coordinator's exact `tests/ui/helpers/{sign-in,clock}.ts` files were copied temporarily for static validation and removed; root `node_modules` was symlinked temporarily and removed.

- `npm run format:check` — PASS, all matched files formatted.
- `npm run lint` — PASS, zero warnings.
- `npm run typecheck` — PASS, Next route types generated and TypeScript emitted no errors.
- `npm run test:unit` — PASS, 3 files / 47 tests.
- `npm run test:ui -- --grep @M2-schedule` — **NOT RUN** in this isolated worker. The assigned worktree has no allocated `.env.local`/runtime, and the coordinator required the integrated root runtime for this browser test.
- Integration tests/build/full verify — **NOT RUN** here; coordinator owns the integrated service/migration branch and reported the new activation suite separately.

## Integration dependencies and next action

- Integrate after shared helper commit `a87c90a` and server preview/migration commit `bf399a5` (or their integrated descendants). The UI consumes the already-frozen `JourneyDraftPreview.reminderTimes` contract and the owner-scoped warning added to `SchedulePreview.warnings`.
- Cherry-pick all three implementation commits, run formatting/lint/type/unit in the integrated checkout, seed the synthetic UI namespace, start its allocated app, then run `npm run test:ui -- --grep @M2-schedule`. Inspect the three-engine results, axe output and real `docs/evidence/M2/<run>/<browser>/weekday-preview.png` before coordinator status changes.
- No contract question remains. UI runtime behavior and evidence remain unverified until the integrated browser test runs, so this handoff does not mark SK-003 DONE.

Owned worktree is clean apart from this handoff before its final documentation commit. No secrets, auth links, shared config, migrations, dependencies, lockfiles or tracking files were changed.
