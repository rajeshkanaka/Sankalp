# SK-005 progress and calendar handoff

Date: 2026-09-06. Owner: `/root/platform_verification`; the coordinator owns task status and integrated verification. Worktree `/Users/rajesh/sankalpa-worktrees/SK-005-progress`, branch `task/SK-005-progress`, base `d7e1d6d`. Coordinator-authorized calendar metrics contract `ab65874` was cherry-picked as `dcd7657`; the coordinator already has that field and should integrate only the subsequent implementation commit.

## Implemented substep

- `src/domain/status.ts` derives independent completion timing: practice during the window or practiced late, plus whether recording happened at/after closing. `metrics.ts` reuses that rule for streak/consistency credit. `src/domain/progress.ts` is the coordinator-approved pure projection shared by server and UI; it preserves practice dates and filters superseded sessions.
- `src/server/progress/service.ts` exports the assigned dashboard, calendar and preference operations. Reads use verified account scope/RLS and a single captured domain clock. Active dashboard journeys are bounded by the existing creation limit. Calendar reads iterate keyset batches of up to 100 journeys, include archived history and never silently truncate the month. Full-journey metrics accompany filtered dates. Selected draft, foreign and nonexistent IDs return the same not-found result. Range validation permits 1–42 inclusive days. Streak writes consume the shared strict scalar schema and the narrow profile permission in migration 007.
- Named UI exports: `ProgressToday` from `src/features/progress/today.tsx`, `JourneyProgress` from `journey.tsx`, and `Calendar` from `calendar.tsx`, with the assigned prop contracts. Coordinator supplies route/navigation wiring and revision editors.
- Reused Midnight Sanctuary palette, serif headings, progress ring and controls. Added canonical totals, optional streaks, consistency, explicit ended/fully-completed wording, nearby/history lists, multiple-journey selector and calendar links. Date attribution and recurrence wording reflect the selected schedule. No mocked integration is labeled complete.
- Calendar supports GET query keys `month`, `journey`, `mode`, `date`, native filtering controls, month navigation, per-journey session links, a semantic grid and chronological list. Full-journey totals remain unchanged by date filtering. Device-local times are secondary text; they never change a calendar cell. All-journey Today uses UTC; a selected journey uses its zone.
- Real-time views request fresh canonical server data at the next opening/closing boundary and when returning to a previously hidden page. Countdown text has no live region. Demo clocks remain externally controlled and require fresh navigation/reload after clock changes. Preference failures retain the previous confirmed value and show the shared recoverable error message.

## Verification evidence

Actual checks in this worktree under Node 24.20.0:

- Scoped ESLint: PASS.
- Typecheck (`next typegen && tsc --noEmit`): PASS.
- Existing metrics plus new progress unit tests: **17 PASS** across two files. Cases cover timing independence at closing, overnight identity, numeric partial/zero status, unscheduled weekdays, correction-driven streak recomputation, ended 20/21 and fully completed 21/21, current-session rollover, superseded rows and date-range validation.
- Scoped Prettier and `git diff --check`: PASS.
- An initial TypeScript circular-inference error on the keyset batch was fixed with an explicit `pg.QueryResult` row type; the subsequent typecheck passed.
- `tests/integration/progress.test.ts`: **NOT RUN** here; nine real-database cases cover filtered/shared totals, civil/overnight identities, owner isolation, invalid ranges, exact closing, superseded rows, drafts, archived history and streak preference isolation/validation.
- `tests/ui/m2-progress.spec.ts` (`@M2-progress`): **NOT RUN** here. It uses real captured local sign-in and authenticated API fixture creation, then exercises overnight date cells, 7/21/33% across Today/Journey/Calendar, preference persistence, multiple-journey and date filters, New York secondary device time, month navigation, axe and 320px list reflow. It retains only synthetic workflow screenshots after authentication; it does not log callback links or tokens.
- App baseline smoke, production build, actual browser screenshots, real-time boundary refresh and manual accessibility: **NOT RUN** in this unallocated worker. Required integrated checks remain the coordinator's responsibility; static/unit checks are not substituted for them.

Dependencies were accessed through a temporary symlink to the coordinator's installed `node_modules`. No dependency installation, environment copy, runtime allocation or database/browser operation was performed. Remove the owned symlink after the final static check; do not stage it.

## Exact next action

1. Integrate the implementation commit, preserving the coordinator's contract and migration 007. Wire the three UI exports and protected `PUT /api/profile/preferences` as planned. Normalize calendar query values in the route before rendering.
2. In the allocated integration checkout run `npm run test:integration -- tests/integration/progress.test.ts`, then the relevant complete verification gates and `npm run test:ui -- --grep @M2-progress`. The integration suite uses its own guarded synthetic Maya/Arun emails, unique clock file and exact ownership markers; it restores the previous clock and cleans only matching synthetic users. UI fixtures use the existing isolated UI namespace and restore its M1 clock in `finally`.
3. Review retained `calendar-33-percent.png` and `calendar-session-list-mobile.png`, verify that real boundary refresh and returning to a hidden tab update counts without repeated screen-reader announcements, and rerun M1 completion assertions plus SK-003/004 UI checks. Any failure remains unfinished until fixed and reverified.
4. Coordinator records actual results, evidence/commit references and task status. This handoff does not mark SK-005 or M2 complete. No worker process is expected to survive the session.
