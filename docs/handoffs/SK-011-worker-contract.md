# SK-011 worker contract

Frozen2026-09-06; SQL implementation by coordinator follows in migration011. Source type authority: src/server/reminders/worker-contracts.ts. This is a contract, not a claim that functions/runtime work. TASKS owns task status.

Worker owner may implement src/worker/*.ts, tests/unit/worker*.test.ts and docs/handoffs/SK-011-worker.md only, in a fresh isolated branch/worktree. No runtime/dependency/config/migration/shared tracking changes. Use .js relative imports to support coordinator's later NodeNext tsc build. No Next imports. Coordinator supplies scripts/worker build/env and SQL; worker requires only WORKER_DATABASE_URL and runtime values explicitly provided by entrypoint.

Database client is a pg.Pool of restricted app_worker with max5, connectionTimeout5000, statement_timeout10000, idleTimeout30000. Validate current_user=session_user=app_worker, nosuper/nobypass and no memberships on every checked-out connection. One short transaction per method; never retain connection/transaction over transport. No request.jwt.claim.sub. Call only these narrow functions:

- SELECT * FROM app.claim_reminder_jobs($1::timestamptz,$2::int,$3::boolean) returns snake columns job_id,lease_token,lease_until. limit1–4 in caller, database hard cap50. Claims only matching simulated mode; lease60s. Map dates to ISO.
- SELECT app.prepare_reminder_job($1::uuid,$2::uuid,$3::timestamptz,$4::boolean) AS result. Inputs jobId,leaseToken,now,simulated; JSON matches PreparedReminder. Revalidates under parent locks, increments attempts, records dispatch_started. Only ready:true authorizes one attempt; repeat prepare returns false.
- SELECT app.settle_reminder_job($1::uuid,$2::uuid,$3::int,$4::jsonb,$5::timestamptz) AS result. Inputs jobId,leaseToken,attempt, JSON.stringify(PushResult),now. Result boolean, false on stale/invalid fencing. SQL validates result allowlists/mode, enforces at most3 attempts and expiry, handles generation-specific invalidation. Never log this JSON or destination.
- SELECT session_id FROM app.due_session_closures($1::timestamptz,$2::int), max50.
- SELECT app.record_session_closure($1::uuid,$2::timestamptz) AS result, boolean (newly inserted), same unique closure stream as app.
- SELECT app.record_worker_heartbeat(); DB clock_timestamp operational heartbeat independent of demo clock.

Worker runOnce({store,transport,now,simulated,signal?}) returns safe aggregate counters only; no identifiers, endpoint, key, payload, raw errors. Up to50 jobs total, claim at most4 at a time and immediately prepare+send concurrently≤4, settle before next claim. Domain clock injected as now():string each call, simulated adapter receives same clock. Before each send check signal, current expiry and returned mode; cancellation after prepare still settle truthful canceled outcome. Use stable base64url Topic length≤32 derived by SHA256(sessionId ?? jobId,subscriptionId,subscriptionGeneration); same tag in JSON. Payload {version:1,eventId,journeyId,sessionId,subscriptionId,subscriptionGeneration,tag,title,body}; generic title 'Your practice reminder', body 'A moment for your commitment.'; test generic 'Test reminder'. Only explicit detailed opt-in supplies nonnull journeyTitle, truncate safely to keep UTF8 payload≤3072. Never include intention/reflection/auth URL. Test payload has null session/journey and opens settings.

Use actual transport outcomes; simulated result can never become real. Missing/error prepare sends nothing. Ambiguous transport exception after invoking send settles uncertain sanitized reason rather than success; SQL failures leave lease for recovery. Failed settle never fabricate receipt. Run closure sweep independent of dispatch failures, and heartbeat after bounded successful DB work. Return counts useful for testing without private values. Loop repeats30s after prior tick, prevents overlaps, handles SIGINT/SIGTERM via abort+await current bounded tick then closes pool. One-shot returns nonzero on operational failures; ordinary provider failures count as processed outcomes. Entry CLI chooses --once; reject unknown args. APP_ENV/clock/PUSH_TRANSPORT safeguards are coordinator-owned runtime config, pass dependencies into exported loop functions. Production cannot select simulated transport/demo clock.

Tests: successful dispatch and exact call order; no-ready/expired/mode mismatch/abort no send; ≤4 concurrency and ≤50 jobs; next batch only after settle; generic/detailed privacy and bounded Unicode payload; stable tags; transient/uncertain/settle failure honesty; independent closure sweep; non-overlap/shutdown; SQL parameterization/role rejection/date mapping. Deterministic injected stores/transports are simulations. Actual role/SQL/worker integration remains NOT RUN until coordinator runs slot2. Do not call external push.
