# SK-008 core implementation handoff

2026-09-06. Worker branch `task/SK-008-core`, worktree `/Users/rajesh/sankalpa-worktrees/SK-008-core`, base `b3e4a27`. Shared status remains in [TASKS](../TASKS.md). Core ownership is `src/offline/core/**`, focused offline tests, and this report. Coordinator owns actual app/SW/API integration and runtime.

## Frozen integration interface

`src/offline/core/index.ts` will export `createOfflineCore(options?)`, the same methods bound to a lazy default instance, all types from `types.ts`, and `OfflineError` with a safe `code`. The types are the concrete integration contract. Import only this entry point in UI; no server imports.

- After a verified identity, `bindAccount(accountId)` returns `{accountId,generation}`. A new account with pending old work quarantines it and throws `ACCOUNT_CHANGE_PENDING`; show generic recovery/discard options. Explicit `bindAccount(newId,{discardPrevious:true})` clears the previous private namespace. `getDeviceState()` exposes counts and a scope only when local data may be shown. It does not authenticate a browser cookie.
- `saveSnapshot(scope,{session,journeyTitle,reflection,preferences,lastSyncedAt,clock})` stores explicit visited data. `read(scope,sessionId)` returns `snapshot` (canonical), `projectedSession`/`projectedReflection` (pending local feedback), `draft`, `draftRevision`, ordered operations and separate `heads.session`/`heads.reflection`. Never calculate canonical progress from the projection.
- `saveDraft(scope,sessionId,draft,expectedRevision)` preserves raw numeric/note input; returns the new draft revision. Null explicitly clears the draft. Compare-and-swap prevents stale tabs silently replacing text. UI keeps its in-memory text when any call fails.
- `enqueue(scope,{operationId,sessionId,scheduleVersionId,baseRevision,expectedLocalHead,intent})`: allocate one UUID per user action, preserve it on enqueue retries; use `heads.session` or `.reflection` and the matching canonical revision (reflection absence = 0). Intent kinds are practices/completion/completion_undo/reflection with the frozen domain payloads. A success means transaction commit completed, not server acknowledgment.
- `flush(scope,signal?)` is bounded, foreground, one Web Lock leader. UI schedules another foreground attempt at `retryAt`, and on online/focus/reopen/explicit Retry. `not_leader` also returns a retry time. No automatic background service. `subscribe` emits only invalidations; reread before rendering and clear React private state if the scope is invalid.
- Conflicts retain both versions. `refreshConflict` obtains an authorized comparison without changing queue payloads. `resolve` needs the exact reviewed stream operation IDs and draft revision, then either explicitly chooses server or submits one reviewed replacement intent/new UUID against the displayed current revision. Descendants in that stream are discarded only as part of that explicit reviewed choice; the other revision stream remains untouched. Session/version identity cannot change. Resolution also clears only the reviewed stream's raw draft fields.
- `clearAccount(scope,'synced'|'discard_confirmed')` invalidates every existing scope and purges private stores. Synced refuses outstanding drafts/operations. `setSharedDevice(scope,true,action)` performs the same pending-work gate and purge; every tab checks the durable preference before private writes. Disabling returns a fresh scope. Local clearing is never a claim of server logout.
- `getDeviceState/listSavedSessions` support the public offline shell. Feature failures reject with `OfflineError`; no in-memory persistence fallback. Limits: 500 pending operations, 50 unpinned saved sessions; pinned drafts/pending operations are not evicted. Over-30-day operations require review.

Read D15 and root D16. Base reflection validation is older than coordinator's verbatim Unicode fix; core consumes the shared validator and must be integrated onto that corrected source. No duplicate reflection normalization policy is introduced.

Coordinator approved required `Snapshot.clock: {serverNow,capturedAt,simulated}`: offline UI keeps the simulated server instant fixed; real mode adds elapsed device time since capture to preserve the server offset. Queue age uses actual device elapsed time independently; performedAt stays the frozen explicit user action payload.

## Verification checkpoint

Types/API only at this checkpoint. Implementation, unit and real IndexedDB/browser checks: NOT RUN. No root database/environment/runtime was accessed. Next: implement storage, pure queue policy, fixed-route transport and replay, then run scoped static/unit checks and an isolated real-browser IndexedDB harness if available.

## User-requested break checkpoint — 2026-09-06

**WIP; SK-008 is not complete.** The coordinator relayed the user's break request immediately after the first real browser harness run. New implementation stopped. This checkpoint preserves the source and tests for resumption; it is not a completion claim.

Implemented within the owned core: real idb8 storage with transaction-completion waits, account generation guards and quarantine/purge, raw draft CAS, validated intent queue with independent revision streams, immutable attempted requests, own-predecessor acknowledgment propagation, bounded Web Lock replay, safe fixed-route fetch transport, conflict comparison/resolution, 30-day review, pending-work/shared-device gates, ID-only invalidations, and the lazy public entry point. These require further review and integrated testing. `Snapshot.clock` is now required and runtime-validated, as approved by the coordinator/UI worker.

Actual verification:

- `fnm exec --using 24.20.0 npm run test:unit -- tests/unit/offline-model.test.ts`: **PASS, 6 tests**. Pure validation/projection/age/backoff tests; these do not claim IndexedDB coverage.
- `fnm exec --using 24.20.0 npx tsc --noEmit --incremental false`: **PASS** after the full harness source was added.
- Core-only scoped ESLint and Prettier: **PASS** before the browser harness addition. The final expanded ESLint command (`src/offline/core tests/unit/offline-model.test.ts tests/offline-core`) **FAILS, 13 no-undef errors**, all in `tests/offline-core/run-browser.mjs`: browser-evaluated `window`, Node `crypto`, and `console` are not declared in this new standalone MJS harness under the existing lint configuration. No source lint errors were reported. This remains unfinished; do not weaken lint assertions.
- `fnm exec --using 24.20.0 node tests/offline-core/run-browser.mjs`: actual **Chromium PASS and WebKit PASS**, each running basic acknowledgment/projection and independent streams; lost-response exact retry and unrelated-revision conflict; quarantine/recovery/shared-device/purge; 30-day review and 50-unpinned retention; actual document reload; and two-page Web Lock leadership. Both reported zero page errors. These use real browser IndexedDB/Web Locks with an explicitly synthetic injected server adapter, not the actual app/auth/database/service worker.
- The same command **FAILED before Firefox tests ran**. Installed Firefox1543 exited at launch with `Could not find profile folder` for Playwright's temporary profile under `/var/folders/.../T/`. **Firefox cases NOT RUN.** Use the repository's existing Firefox temporary-directory remedy on resumption, after inspecting its current configuration; no browser install or settings change was attempted.
- `git diff --check`: **PASS** at the stop checkpoint.
- Full application build, root database/API integration, actual app offline UI/service-worker/auth/account-switch flow, storage denial/quota/commit-abort simulation, queue500 ceiling, and full regression: **NOT RUN by this worker**. No root `.env` was read/copied, no database action was performed, and no app runtime was used.

Known review item before integration: `resolve` currently applies both snapshots from its saved conflict comparison. Add a regression for a newer canonical snapshot or independent-stream acknowledgment arriving after that comparison, then preserve the newer canonical revision or require fresh comparison; never regress a snapshot by choosing an older saved comparison. No fix was attempted after the break request. Also review transport malformed-response handling, account/auth cancellation, stale-tab resolution boundaries and the missing storage-failure/500-operation tests before promoting this WIP.

Owned runtime has stopped: the harness used a random loopback HTTP port and headless Chromium/WebKit, with its server/browser cleanup in `finally`; the command exited after Firefox's launch failure. No owned surviving app/DB processes are expected. Generated harness assets remain ignored in `.local/offline-core-browser/`. The worktree has an intentionally untracked `node_modules` symlink to `/Users/rajesh/sankalpa/node_modules` for static checks; do not stage it or treat it as a dependency install. No secrets, real notes, auth links or screenshots were created.

Exact next substep after the user's resume instruction: read this checkpoint and current coordinator tracking; inspect branch/Git state and integrated validation fixes; correct only the harness environment declarations, rerun its static gates, then add the stale-comparison regression/fix and missing failure/limit tests. Rerun the isolated real-browser harness using the existing Firefox environment convention. Hand the reviewed commit to the coordinator/UI worker, then let the coordinator run actual app/SW/auth/database offline integration and regression. Until those pass, this remains WIP.
