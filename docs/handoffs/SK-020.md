# SK-020 — README showcase and one-command demo

User requested the README/showcase during the emergency pause, then explicitly added a setup script and clarified real authentication. This is a bounded presentation/tooling task. Application feature work remains paused; SK-008-P1 is the first feature resume action. TASKS alone owns status.

## Ownership and changes

Original implementation: coordinator `/root`, root checkout `/Users/rajesh/sankalpa`, branch `rajesh_kanaka/emergency-checkpoint`, base `638be75` above accepted application `23f15b2`. Read-only independent review by `/root/branch_audit`; no overlapping worker writes. This source is now accepted on main by PR6; the branch remains a historical ref.

- README front page now explains the concept, working/unfinished capabilities, one-command setup, real-auth/captured-delivery distinction, screenshots and navigation.
- `setup.sh` selects pinned fnm Node/npm and runs built-in-only `scripts/setup-demo.mjs`. The latter installs dependencies with development tools, checks local Docker/resource ownership, starts/migrates the local backend, preserves ready fixtures or explicitly seeds, builds, checks readiness and opens app/inbox on macOS.
- Existing app/test/proxy ports prevent destructive dependency/build overlap. A per-checkout lock spans setup and app lifetime. All inherited runtime connection settings are stripped in child environments; generated owned local configuration wins. Only local Docker sockets are accepted. New slots exclude stopped Docker resources and registered worktrees.
- Explicit `--reset-demo` replaces marked synthetic data; default existing metadata/clock is preserved. Missing/incomplete existing metadata fails without guessing that the database is empty.
- Ctrl-C starts bounded termination of only the script's process group immediately, including an ignoring leader or surviving descendant. It awaits cleanup before releasing its lock; database data persists.
- `docs/DEMO.md` owns the exact presenter, reset/recovery and manual fallback instructions. No application source, migration or dependency manifest changed.

## Verification and evidence

[Manifest](../evidence/demo-launcher/manifest.md) and safe functional JSON are authoritative for the observed runs and limits. Actual isolated clone: `/Users/rajesh/sankalpa/.local/demo-check-jqo36hoh`, slot 3 (app 3003, tests 3103, API 54621, DB 54622, inbox 54624). It is an ignored verification clone, not another implementation branch or worktree. Root slot 1 and unfinished M4 slot 2 were not reseeded or rebuilt. The isolated slot3 app was stopped after verification; its backend stop is recorded in the session log, with volumes retained. Local raw log: that clone's `.local/final-verify.log`; private setup details in `.local/setup.log`.

The initial showcase passed 16 launcher tests, 232 unit tests, 84 database tests, production build and real Chromium smoke. Fresh setup and repeat preservation used real local services; README rendering, image provenance and guarded failures were inspected. PR review corrections then passed 20 launcher tests and 236 unit tests locally. Final hosted CI passed 236 unit tests, 84 database tests, 98 actual UI tests and both offline harnesses. The manifest keeps these distinct source/run checkpoints and the actual setup repeat after the review fix.

Independent review identified and then cleared inherited configuration leakage, test-server overwrite risk and shutdown escalation gaps. Tests retain direct regressions, including an exported DATABASE_URL sentinel, a SIGTERM-ignoring child after leader exit and a server leader that itself ignores SIGTERM. No security control or test assertion was weakened.

## New calendar finding: SK-005-P2

Real Chromium reproduction: Calendar → September2026 → Morning grounding → leave Practice date (optional) blank → Apply filters. Result: Choose calendar dates. The GET form submits `date=` while `src/app/(private)/calendar/page.tsx` accepts only omitted or valid date. All journeys also submits an empty `journey` against optional UUID validation. The guide supplies an explicit journey/date; actual grid/list verification then passed.

On feature resume after urgent P1, normalize only genuinely empty optional fields before strict validation, retain rejection of malformed/nonempty dates/IDs and invalid ranges, and add real form-submit regressions for all-journey month overview and journey-only filtering. This defect is recorded without implementing application code during the break.

## Exact next action and restart

Final source `f42f1c1` passed [CI 34031227206](https://github.com/rajeshkanaka/Sankalp/actions/runs/34031227206). All review findings were addressed; both threads resolved and latest review recommended approval with no new comments. [PR6](https://github.com/rajeshkanaka/Sankalp/pull/6) normally merged as **`ff6a479` at 12:10:50 UTC on 2026-09-06**. Local main fast-forwarded cleanly, and GitHub main's README/setup/DEMO blobs matched it. CI's temporary merge checkout tree exactly matches the actual merge tree; safe aggregate evidence is in the manifest. No administrator bypass or history rewrite.

Keep feature pause until the user resumes. Then fetch main's checkpoint, fix SK-008-P1 first, address SK-005-P2, and return to the preserved M4 reports. Do not restart this completed presentation work or interpret demo acceptance as launch completion. The coordinator's subsequent documentation PR records this observed acceptance and corrects stale paused-work pointers; its own commit identity is available from Git history without changing the accepted application checkpoint.

For the maintainer presentation: `cd /Users/rajesh/sankalpa` then `./setup.sh`; if existing3001 is already running, open its printed address instead. Root startup must not copy slot3's environment. For deliberate reset, first save/sync/sign out and follow DEMO before `./setup.sh --reset-demo`. No process is assumed to survive.
