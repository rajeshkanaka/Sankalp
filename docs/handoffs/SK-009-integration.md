# M4 coordinator integration checkpoint

**Current paused checkpoint: `4d9fd1d`; use the Emergency pause section below.** The initial foundation paragraphs retain earlier evidence, not current instructions. Migration 011 is applied; corrections require migration 012. PR6 later published the presentation tooling and saved context on main without accepting unfinished M4 source. Feature resume order is owned by PROJECT_PROGRESS.

2026-09-06. Worktree SK-009-integration, branch rajesh_kanaka/reminder-integration, isolated slot2. Accepted base544f3ef; merge PR5's accepted main before cumulative application checks. TASKS alone owns status.

Reviewed source integrated: pure domain2b66431/4c121ec; preference contract67c4704 and version/validationb817885; form3aa243b; database tests0a3a04a; guarded transport e296259. Transport includes212 focused tests and actual local TLS exercises, no real push.

Migrations001–010 are now applied to slot2 (API54521/DB54522/mail54524).010 creates restricted roles, subscription/job/history tables and privileges only; narrow functions and worker runtime remain unfinished. Independent SQL review findings were fixed before first application: locking privileges, event/job context, one attempt outcome, role-membership drift and lease-state constraints. Existing history reads explicitly select legacy session events. Strict seed inventory now includes the four added tables and still refuses unknown schema/data.

Verification:404 unit tests passed after moving six form model tests into the existing Vitest gate. Initial full DB run271 passed/1 failed because the seed safety guard correctly rejected newly introduced tables. After updating its exact inventory, targeted seed recovery1/1 and full DB272/272 passed; typecheck passed. Logs: docs/evidence/M4/schema-foundation/. Slot2 baseline production smoke screenshot is in docs/evidence/M1/2026-09-06T09-20-37-137Z/. No worker/API/device/UI reminder claim is made. Role-specific and new schema tests remain to be added.

Next: integrate preferences no-op fix305a6bb (preserve its added tests in tests/unit/reminder-preferences-model.test.ts), add/test narrow lifecycle functions in a new migration011, assign bounded worker client/loop against frozen signatures, wire settings/device/history UI and actual browser regression. app_worker has no configured password/runtime URL yet. Keep secrets and node_modules untracked. Restart slot2 with fnm use24.20.0; npm run db:start; npm run build; npm run test:smoke. App port3002; no surviving app process assumed. Local/CI reminders must remain explicitly simulated.

## Emergency pause — 2026-09-06 15:52 IST

User requested immediate break and durable save. Stop implementation. **First resume task is PR5 P1**, as described in SK-008-independent-review.md: navigation policy rejects legitimate Today/calendar query parameters. PR5 was already normally merged as main23f15b2 after CI34025184143 succeeded before the outside review arrived; no P1 fix or reproduction has been done. Create a corrective feature branch from main, reproduce actual offline Done-link navigation, preserve strict asset policy, fix validated navigation policy, retain real browser evidence and merge only after review/CI. M4 waits.

M4 source: accepted main merged3be8815; settings API/client/config checkpointf405c1c; schema tests c619268 and actual UI-test specification0a34cc2 integrated. **Migration011 is APPLIED to slot2**, despite earlier progress text saying draft. Do not edit010 or011; corrections must be new012. Actual first011 test run:65 passed/9 failed out of74 (schema66 +lifecycle8). Failure evidence remains ignored local artifacts/reminder-lifecycle-first.log in root. Known blockers: PL/pgSQL auth.uid() requires auth schema USAGE which the NOLOGIN owner does not have (existing RLS expressions are pre-bound); pure-helper admin probe lacks EXECUTE, and two schema owner probes cannot SET ROLE from restricted Supabase postgres. Correct actual role boundaries/test-scoped rollback probes without weakening runtime permissions. Lifecycle source review additionally found prepare must reject p_now before scheduled_at/next_attempt_at. Actual rolled-back SQL compilation and migration succeeded; this is not functional readiness.

011 already fixes legacy notification_event_check1 selection and restores named recorded timestamp order; claim remains job-only to avoid implicit parent FK deadlocks, recovery_pending preserves uncertainty, prepare/settle take account→journey→session→subscription→job, nullable-attempt fencing and recovery-before-refresh-cancel were reviewed. Ownership transfer temporarily enables only the migration login's SET/INHERIT and restores prior options; verify actual final grants. Verified official PostgreSQL17 GRANT/ALTER FUNCTION docs2026-09-06: https://www.postgresql.org/docs/17/sql-grant.html and https://www.postgresql.org/docs/17/sql-alterfunction.html.

Pending independent commits, not integrated: schema recorded-order regressiona57388b (67 cases); workerfd613d4 (82 focused/489 full unit +static PASS in isolated branch, actual SQL/worker/build NOT RUN); preferences checkpoint e26bffe (report only, bridge source NOT STARTED). Their branches/worktrees are in BRANCHES. Preserve all. The settings page still needs PageHeader/h1 and actual sanctuary CSS module classes; activation/revision/completion/undo refresh hooks are not wired. The form needs the approved narrow frozen/registerEditor lifecycle bridge to protect unsaved/invalid/pending input on sign-out/shared-mode. Browser12 cases are discovered, NOT RUN. No actual reminder send/device integration occurred.

Restart after P1: this worktree uses fnm use24.20.0, npm run db:start, npm run db:migrate only after reviewing new012; npm run test:integration -- tests/integration/reminder-jobs.test.ts tests/integration/reminder-schema.test.ts. Then build/type/lint and actual preference UI. Slot2 API54521 DB54522 mail54524, app3002 UI3102. Restricted WORKER_DATABASE_URL generated in ignored .env.local; never print/copy. No app/worker running in slot2; databases retain volumes. Root demo3001 was last running. Assume no process survives; legacy slot0 untouched.
