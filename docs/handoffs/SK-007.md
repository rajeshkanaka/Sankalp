# SK-007 private reflections and journal

Owner: `/root/bootstrap_audit`

Worktree: `/Users/rajesh/sankalpa-worktrees/SK-007-journal`

Branch: `task/SK-007-journal`

Base: `a65b68f`

## Implemented

- Added owner-scoped reflection reads and writes using the shared journey/session lock order and one post-lock clock sample. Writes accept opened sessions on active or archived journeys, reject unopened and superseded sessions, use an independent reflection revision beginning at zero when absent, and return safe acknowledgements without note or mood content.
- Added exact idempotent retries, changed-body operation reuse rejection, no-change rejection, and authorized conflict state. Reflection rows and ordered mood rows update atomically in the existing `withUser` transaction.
- Added private journal search with strict query validation, owner-private journey IDs, parameterized literal substring matching that escapes backslash, percent and underscore, exact mood/date/journey intersections, `(practice_date DESC, session_id DESC)` keyset pagination, no total count, and 280-Unicode-codepoint previews. Empty reflection rows retain revision state but stay out of journal results.
- Added reflection-prompt preference reads and writes for the two frozen prompt identifiers.
- Added a plain-text `ReflectionEditor` with exact note preservation, Unicode codepoint counting, five custom mood tags, optional prompts, 900 ms debounced autosave, explicit save, stable transport retries, and visible pending/saved/error states. Autosave stops after a transport failure until an edit or deliberate retry.
- Added a two-device conflict view that validates the authorized current record, keeps local and saved note/mood versions visible, and requires “Keep my version” or “Use saved version.” Neither path silently merges content.
- Added `Journal` with JSON POST filters, cursor loading without duplicate entries, plain-text/whitespace-preserving previews, and links back to owned session details. Added a separate `ReflectionPreferencesEditor` for prompt choices.

No rich text, HTML rendering, public indexing, AI interpretation, session mutation, shared route, migration, navigation, or tracking changes were made.

## Public UI interfaces

- `ReflectionEditor({ session: SessionRecord, initialReflection: ReflectionRecord | null, preferences: ReflectionPreferences, now: string })`
- `Journal({ initialPage: CursorPage<JournalEntry>, journeys: { id: string; title: string }[] })`
- `ReflectionPreferencesEditor({ initialPreferences: ReflectionPreferences })`

All are exported from `src/features/journal/index.ts`. All five service functions are exported from `src/server/journal/index.ts`.

## Commits

- `5ab41c2` — `SK-007: add private journal services`
- `e914a7e` — `SK-007: cover reflection and journal boundaries`
- `bde90ae` — `SK-007: add journal and reflection recovery UI`
- `d07ad28` — `SK-007: keep operation reuse payload within limit`

## Verification

- `fnm exec --using 24.20.0 npm ci` — PASS; 255 packages audited, 0 vulnerabilities.
- `fnm exec --using 24.20.0 npm run format:check` — PASS.
- `fnm exec --using 24.20.0 npm run lint` — PASS, zero warnings.
- `fnm exec --using 24.20.0 npm run typecheck` — PASS.
- `fnm exec --using 24.20.0 npm run test:unit` — PASS, 7 files and 81 tests.
- `fnm exec --using 24.20.0 npm run build` — PASS.
- `git diff --check` — PASS.
- `npm run test:integration -- tests/integration/reflections.test.ts` — NOT RUN in this worker because the coordinator owns migration 008 and the root database. Coordinator's first integrated run passed four of five cases; the fifth stopped at a test input that exceeded validation before its intended operation-reuse assertion. `d07ad28` keeps that alternate input at exactly 20,000 code points. The corrected case awaits a root rerun.
- `npm run test:ui -- --grep @M3-journal` — NOT RUN in this worker because the coordinator owns routes, page mounts, seeded M3 runtime, and the production test server.

## Coordinator integration

1. Root has integrated `5ab41c2`; cherry-pick `e914a7e`, `bde90ae`, `d07ad28`, and the final handoff commit.
2. Keep the coordinator's verbatim reflection-text schema: preserve leading/trailing whitespace, cap by Unicode code points, and reject NUL/unpaired surrogates. Mood and journal-search strings remain trimmed. The integration and browser tests intentionally assert leading/trailing paragraphs survive.
3. Keep the coordinator's `handleApi` change that serializes an explicit `current: null` for an absent-reflection revision conflict.
4. Wire the strict GET/PUT reflection route, POST journal-query route, and PUT reflection-preferences route. Mount `ReflectionEditor` on the owner-authorized session page and `Journal` plus `ReflectionPreferencesEditor` on `/journal`; add navigation through coordinator-owned files.
5. Apply migration 008 only through D15's guarded empty-history procedure and with the integrated SK-006 session writer.
6. Rerun `tests/integration/reflections.test.ts`, then `npm run test:ui -- --grep @M3-journal` against the production build. The UI case uses only synthetic data, scopes conflict alerts by their expected message, verifies search terms stay out of URLs, and writes `docs/evidence/M3/<run>/<browser>/journal-saved.png` without authentication URLs or tokens.
7. Run the full required verification gates and retain the M3 evidence manifest.
