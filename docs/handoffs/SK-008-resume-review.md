# Offline core resume review

2026-09-06, read-only review of preserved core86769b5 by `/root/branch_audit`; coordinator accepted these as the first unfinished substeps. No tests were executed by this review and no offline completion is implied. See [SK-008](SK-008.md) and [BRANCHES](../BRANCHES.md) for current assignment/context.

1. **Monotonic canonical state.** `src/offline/core/core.ts` resolve around681 unconditionally applies both saved comparison snapshots. A session conflict resolved after a reflection acknowledgment can erase newer canonical reflection state; the reverse affects completion. In the resolution transaction, reject stale reviewed-stream canonical revision, retain queue/draft intact, and merge the untouched stream monotonically. A stale null reflection cannot replace a newer saved note. Preserve direct predecessor acknowledgment semantics; never silently rebase a descendant onto an unrelated newer revision.
2. **Bind the choice to the displayed comparison.** Operation IDs and draft revision alone do not detect another tab refreshing a comparison. Coordinator contract refinement: add opaque `comparisonId` to stored `Conflict` and required `expectedComparisonId` to both `Resolution` choices. Core allocates a new token whenever it installs a comparison; validate the token transactionally before destructive resolution. UI passes exactly the token displayed. This implements existing explicit-review requirements without a stack/scope change.
3. **Account change means quarantine immediately.** Core catches `ACCOUNT_MISMATCH`, but the actual HTTP guard returns `ACCOUNT_CHANGED`. On that response from send or comparison read, invalidate private views, preserve pending data, stop replay and quarantine without depending on a second successful identity fetch. Returning to a reverified original account can recover its pending work.
4. **Serialize destructive account switching with replay.** `bindAccount(...,{discardPrevious:true})` bypasses the replay exclusion used by clear/shared-device. Quarantine immediately; take the previous account's replay lock for purge and recheck generation/identity/pending work transactionally. A busy replay needs a recoverable result. Aborting fetch never proves an already-sent request was canceled on the server.

Required real IndexedDB/Web Locks harness regressions (synthetic transport remains visibly labeled): session conflict then reflection ack; reflection conflict then completion ack; newer same-stream snapshot rejects both old resolution choices; two-tab comparison A→B invalidates A's choice; ACCOUNT_CHANGED plus unavailable identity still quarantines with no further sends; paused in-flight send versus discard/switch; transaction abort yields all-or-nothing enqueue/draft/resolve/purge;500pending limit rejects501without loss; malformed/lost responses preserve exact attempted envelopes. Existing age/50snapshot tests do not prove the500queue limit.

Finish browser/Node globals in the isolated MJS harness and adopt the existing root Firefox `MOZ_APP_DATA` convention. Run Chromium/WebKit/Firefox against actual browser storage; afterward coordinator must separately verify the real app/auth/API/public-shell offline workflow. Harness success cannot establish that integration.
