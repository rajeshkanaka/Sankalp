# Sankalpa decisions

Decision session: **2026-09-05–06, Asia/Kolkata**. These are the single selected path submitted for plan approval, not claims of implementation or user approval. Approval is recorded only in [PROJECT_PROGRESS](PROJECT_PROGRESS.md); task status only in [TASKS](TASKS.md). Product details remain in [APP_SPECIFICATION](APP_SPECIFICATION.md).

## D01 — One web application and one reminder worker

Use a mobile-first **Next.js App Router / React / TypeScript** PWA, with CSS Modules and shared CSS variables. Use managed **Supabase Auth and PostgreSQL**, a small separately runnable **Node.js worker**, and a durable PostgreSQL job table. Use **npm**, one package/lockfile, SQL migrations and `pg` transactions; Supabase JS/SSR clients handle authentication. No ORM, Redis, separate frontend API service, realtime subscription dependency, native app, AI SDK, or agentic application runtime.

This follows the existing specification's direction while making it definite. Managed email-link auth avoids implementing identity. PostgreSQL supplies transactions and durable jobs. A continuous worker supports minute-level dispatch without tying reminders to browser tabs or request execution. Plain React/CSS keeps the calm design editable without a large UI framework. Browser IndexedDB with `idb` is added only for the required offline queue. Temporal's polyfill is used consistently for explicit timezone resolution; native Temporal availability is not assumed.

## D02 — Version selection and reproducibility

The following versions were read from official package-publisher metadata on the npm registry on **2026-09-05–06**, with compatibility checked against the official sources below. They are **selected pins, not installed dependencies**. First implementation creates exact dependency versions, `package-lock.json`, `.node-version`, `.nvmrc`, npm `packageManager` and a Linux container pinned by digest. Do not run a scaffold over existing documentation or use `@latest` in reproducible setup.

| Component | Selected version | Compatibility/maintenance evidence |
|---|---|---|
| Node.js / bundled npm | **24.20.0 / 11.19.0** | Node 24 is LTS; release index records this patch and bundled npm. Local machine currently has older tooling; installation is future work. |
| Next.js / `@next/eslint-plugin-next` | **16.3.4 / 16.3.4** | Next 16 Active LTS; Node ≥20.9, TypeScript ≥5.1; React 19 accepted by peer range. |
| React / react-dom | **19.2.8 / 19.2.8** | Matching versions; react-dom peer requires React ^19.2.8. |
| TypeScript | **6.0.3** | Official 6.0 release docs; supported by typescript-eslint ≥4.8.4 and <6.1.0. Registry latest 7.0.2 is deliberately not selected. |
| ESLint / typescript-eslint / Prettier | **10.10.0 / 8.69.0 / 3.9.6** | Parser supports ESLint 10 and TS 6.0. Use Next plugin directly with `@eslint/js` **10.0.1** and `eslint-plugin-react-hooks` **7.1.1**; no `eslint-config-next` bundle because its react/jsx-a11y peers exclude ESLint 10. Next build no longer runs lint. |
| Supabase JS / SSR / CLI | **2.115.0 / 0.12.6 / 2.116.0** | JS requires Node ≥22; SSR peer ^2.114.0 is satisfied. CLI local services need Docker. |
| PostgreSQL | **17 major** | Supported local CLI template; match production major and verify actual server version during setup. Exact managed patch/image digest is not yet verified. |
| `pg` | **8.23.0** | Node ≥16; native pg binding unnecessary. |
| Zod | **4.5.4** | Stable 4.x, TypeScript ≥5.5; strict mode required. |
| `@js-temporal/polyfill` / `idb` | **0.5.1 / 8.0.3** | Publisher repositories and packages verified; no formal LTS promise. Test timezone and offline behavior explicitly. |
| `web-push` | **3.6.7** | Publisher library; Node ≥16. No delivery guarantee or formal LTS promise. |
| Vitest / Vite | **5.0.0 / 8.2.2** | Vitest permits Vite ^8; Node 24 satisfies both. Vite is test tooling, not a second application bundler. |
| `@playwright/test` / `playwright` | **1.63.0 / 1.63.0** | Node 24 and macOS ≥14 supported; pin matching browser revisions. Playwright also supplies Chromium PDF rendering. |
| PDF.js test extraction / deletion ledger client | `pdfjs-dist` **6.3.289** / `@aws-sdk/client-s3` **3.1127.0** | Node 24 satisfies their declared engines. PDF.js is dev-only; S3 client is server-only and introduced in SK-014. |
| `@axe-core/playwright` / `tsx` | **4.13.0 / 4.23.13** | Accessibility assertions in Playwright; tsx runs Node scripts/worker, Node ≥18. |
| Type declarations | `@types/node` **24.13.3**, `@types/react` **19.2.18**, `@types/react-dom` **19.2.7**, `@types/pg` **8.23.1**, `@types/web-push` **3.6.4** | Node types deliberately follow runtime major 24. Resolve and lock transitive peers during SK-001. |

These sources verify published versions and declared engine/peer compatibility, **not an installed build**. No dependency resolution, runtime combination, browser download or test was executed during planning. SK-001 must run installation/build/lint/type/UI proof without `--force` or `--legacy-peer-deps`; an unexpected incompatibility blocks the affected work and requires a recorded narrow correction. No official maintained-version policy was found for every small library; verify release/advisory metadata before first installation and release. No claim of indefinite maintenance is made.

For first install, use these pins after checking advisories. Once locked, ordinary safe patch updates within these selected component lines can be proposed in focused maintenance tasks with evidence; user approval is required for changing the selected stack, major/minor baseline or architecture. Do not silently switch a component to fix an install failure.

## D03 — Data access and security boundaries

Use same-origin route handlers and server-only `pg` transactions. Browser clients use Supabase for auth only; do not grant browser roles direct access to application tables. Migrations create an `app_api` database login/role with only required table/function privileges and RLS; it has no BYPASSRLS, superuser or schema-owner rights. Each request verifies the Supabase user on the server with `getUser`, then starts a transaction, sets the verified subject via transaction-local claims, and uses RLS based on that subject. Always commit/rollback before releasing a pooled connection. Never accept the claims/owner from a request body. `auth.uid()` reads the transaction-local claims. Explicit GRANTs are required; RLS alone is insufficient.

The migration owner is an offline administrative credential. The worker role can execute narrowly scoped claim/revalidate/settle/closure/deletion functions, with fixed search paths; it cannot read reflections or arbitrary user tables. Supabase admin credentials are isolated to account deletion/auth administration and local synthetic-data setup. Runtime account deletion remains server-only. Private routes are dynamic and `no-store`; neither shared caches nor the service worker store authenticated HTML/API responses.

## D04 — Schedule and product clarifications

These clarify the existing specification and are included in the requested approval. They are not approved changes yet.

1. Keep all existing launch features and A01–A27. English UI at launch; arbitrary Unicode practice names/reflections, including Devanagari. Audience is individual adult practitioners; no child/family, social or religious-advice model is introduced.
2. Start custom journeys blank. One session slot per journey; independent morning/evening practices use separate journeys. Start activation requires at least one session whose window has not opened; past reminder offsets are skipped. The fixture/test clock may make a same-day session open for deterministic demos.
3. **Partial means any nonzero recorded progress** on a required practice, or all targets met without confirmation. Thus 5/20 minutes remains Partial after closing, not Missed. An all-zero/unconfirmed checklist is Open then Missed. This resolves the numeric-progress ambiguity in specification §5.2; no partial completion credit is added.
4. Windows use a chosen **local wall-clock duration**, 1–1,439 whole minutes. Compute nominal closing local datetime, resolve both boundaries, then reject nonpositive, ≥24 elapsed-hour, or overlapping windows. Repeated local times choose the first occurrence; nonexistent times resolve to the first valid instant after the gap. Do not substitute Temporal's default gap-shift behavior (02:30 → 03:30) for the required first valid 03:00. Record/preview every adjustment.
5. All practices are required at launch. Checkbox values are booleans; repetition/minute targets are positive integers up to 1,000,000/1,439 respectively, recorded values are integers 0–1,000,000. Title 1–120 chars, intention ≤4,000, practice name 1–120, reflection ≤20,000, custom mood ≤40, maximum five mood tags. Trim surrounding whitespace; preserve Unicode and internal text. No arbitrary HTML.
6. Reminder offsets are signed minutes in [−1,440, 0], up to eight unique offsets per journey; disabled by default. Quiet hours use the journey zone, allow crossing midnight, and suppress rather than move reminders. Snooze is the separately identified +10-minute job, permitted only strictly before closing. Up to five active device subscriptions and 20 active journeys per account keep launch workloads bounded; archived history remains available.
7. Superseded sessions remain tombstones for linked reminder history. Use partial unique indexes on `(journey_id, practice_date)` and `(journey_id, ordinal)` **where not superseded**, preserving historical IDs. New schedule versions cannot change opened sessions, even if nobody visited them.
8. Reminder job identity includes session, schedule version, **reminder preference revision**, subscription generation and offset/kind. Snoozes have a distinct idempotent request key. Dispatch revalidates all generations. Click reporting proves a received application click report, not externally verified reading.
9. Optional journey-closing reflection is an **export-time text field** only, not a second persistent journal model. Private reflections are plain text. Mood tags remain optional. Export defaults exclude reflections until selected.

## D05 — UI tests, offline and deterministic demonstrations

Use **Playwright Test** for every browser workflow, screenshots, downloads, offline simulation and accessibility checks. Vitest covers pure domain functions, Node services and real PostgreSQL/API integration; no second browser testing framework. All app-level tests exercise real local auth/database. A fake **external push transport** is allowed in local/CI and labeled “Simulated; no device notification sent.” Local auth email is captured in Supabase's mail inbox; it is a real local auth flow, not production email delivery.

Use deterministic synthetic fixtures and a server-injected test clock in local test mode only. Use `APP_ENV=local|ci|staging|production` independently of `NODE_ENV`. A Next production build may run guarded local/CI fixtures against allowlisted disposable resources. Staging/production startup rejects fixture-clock/fake-auth configuration; CI verifies those guards. Supabase Auth expiry/signatures always use actual time, never the demo domain clock. Browser and worker receive the same clock through their test adapters; server-side timestamps remain authoritative in normal mode. Playwright clock alone cannot fake database/server time.

Offline uses explicit account-scoped IndexedDB records and an idempotent mutation queue. No claim of encrypted local storage or guaranteed background sync. Disable local private storage on shared devices. Clear private data and revoke the account's push registration on logout/account change after handling unsynced work. Functional assertions are mandatory; screenshots are supplemental evidence.

## D06 — PDF, fonts and sound

Generate downloadable PDFs with **server-side Chromium via Playwright** from an authorized consistent snapshot. This chooses one path before export implementation and avoids uncertain browser PDF-library shaping of Devanagari. Render app-owned escaped HTML with bundled Noto fonts, JavaScript disabled, outbound requests denied and no user URLs. Use ephemeral bytes, attachment response and no-store headers; never a public object URL. One render at a time per web process, bounded queue, 15-second timeout, bounded 365-session export and cleanup on all exits. A resource-constrained render returns a recoverable busy/error state.

Self-host Noto Serif, Noto Sans and Noto Sans Devanagari with exact source revision/hash and OFL notices in the future asset manifest. The Noto Devanagari repository has OFL licensing, but selected font files/subsets have not been downloaded or visually validated. Ship three **original** non-sacred ambient loops (rain, flowing water, drone) plus silence; SK-013 creates or obtains original assets with documented authorship/source rights. No unlicensed recordings or claims of ritual authenticity. Audio/font evidence is required before that task is DONE. No assets are created in this planning turn.

## D07 — One deployment path and its approval gates

Select **Render Singapore**: one paid always-on Docker web service (`1c-2g`, including restricted Chromium PDF) and one continuous background worker (`0.5c-512mb`); **Supabase Pro in Singapore (`ap-southeast-1`)** for Auth/PostgreSQL; **Resend custom SMTP** for auth mail from an owned verified sending domain. Add one private **Amazon S3 Standard general-purpose bucket in ap-southeast-1** solely for the independent deletion ledger specified in D08; no app content or exports live there. No Redis or separate PDF service. Render and Supabase communicate over authenticated TLS across providers, not an assumed private network. Deploy staging first, then production, as separate databases/auth/projects/configuration and explicit user-authorized operations. Never share synthetic staging data with production.

Supabase Pro starts at $25/month with the first Micro compute included in documented pricing. Exact Render instance prices, additional staging/database compute, email volume, S3 requests/storage, tax/domain/egress costs and complete monthly total were **not verified**. Present a live itemized quote and budget cap before provisioning; this selection does not authorize spending. Do not promise a free production deployment. Configure deploys manually from approved immutable revisions with automatic deploy disabled. Docker image digest, regional resource availability, Chromium runtime limits, SMTP/domain ownership and database patch remain runtime/provisioning gates.

Production recovery: backward-compatible expand/contract migrations, separate migration step, previous image rollback, worker pause before restore, isolated restore rehearsal and reconciliation before reminder restart. Never automatically reverse a destructive migration. Targets: ≤24h data-loss window from daily backups and ≤4h service recovery after operator response; these are proposed operational objectives to measure, not SLAs.

## D08 — Privacy and retention

No advertising/behavioral analytics or AI processing of reflections. Retain user-visible history while the account exists; journey deletion removes its content-bearing events. Operational logs are separate, redacted and expire within seven days. Deletion immediately disables access/pending sends, deletes primary owned content within 24h, and targets backup expiry within 30 days. Supabase Pro's documented seven-day daily-backup availability does **not** independently prove end-to-end erasure from all provider systems. SK-016 must establish the actual provider retention/deletion policy and any needed contract/disclosure before external beta. Unresolved compliance is a release blocker, not permission to silently weaken the promise.

The independent ledger is a private S3 bucket (separate staging/production prefixes), with public access blocked, TLS-only policy and default server-side encryption. Each object contains only opaque account UUID, deletion operation UUID and accepted deletion time; never names, email or spiritual content. Database deletion first atomically disables access/sends and writes an outbox item. A narrowly scoped server credential conditionally writes the immutable ledger object (`If-None-Match: *`) and verifies its receipt before primary/auth cleanup completes; retry is idempotent. A ledger outage preserves the disabled state and triggers the 24-hour cleanup failure alert, never a false deletion-complete message. Database restoration cannot roll this bucket back.

Worker ledger credentials are limited to the designated prefix; separate operator read/list credentials restore deletions before reopening any recovered database. Use no object versioning/replication; schedule expiry at 29 days, add an explicit daily expiry sweep and verify logical removal by 30 days. S3 Lifecycle processing is asynchronous, so do not claim its rule alone guarantees a hard physical-erasure deadline; provider retention still requires B03 confirmation. Local/CI uses a visibly simulated filesystem ledger **outside the reset database**, testing the same receipt/failure contract; real S3 policy/restore/expiry is required in SK-016/018. Missing authoritative ledger blocks reopening restored data. This small store is necessary for safe recovery after account deletion; it is not a general storage service for the app.

Production privacy text must disclose actual policies, local storage limits, reporting-based accountability, notification limitations and support contact. No end-to-end encryption claim.

## D09 — Approval and change procedure

The user requested these documents and a local planning commit on 2026-09-05. No implementation, spending, staging, real messages or production deployment is authorized in this session. Record actual plan approval with date and scope in PROJECT_PROGRESS and a dated SESSION_LOG entry. Then these selected decisions become the baseline. For a later change, append a decision with problem, exact change, impact/migration/tests and the user's approval reference; preserve superseded decisions. Routine substeps do not need repeated approval within already-authorized scope.

## D10 — Approved continuous implementation (2026-09-06)

User approved the entire plan and instructed the coordinator to commit, push, orchestrate agents and continue until the app is complete. This authorizes implementation, dependency setup, focused commits/pushes and progression across milestones without repeated visual-review pauses. Retain all technical acceptance, security, real-device and evidence gates; do not record waived visual pauses as reviews that happened. The coordinator may assign bounded substeps within a task to isolated workers while retaining single-task accountability.

Deployment work is authorized in principle as part of the approved plan; actual budget cap, accounts, domain/sender, device availability and recipient consent still require concrete inputs. A concise question for those details was presented while local implementation proceeds. Do not spend without an established cap or fabricate credentials/device results. No unrelated destructive operation or shared-history rewrite is authorized. Subsequent architecture/stack changes retain the existing approval rule; compatibility corrections must be evidence-backed and recorded.

## Verification sources

Accessed during **2026-09-05–06 (Asia/Kolkata)**; version support/package metadata was rechecked on 2026-09-06 for the final compatibility corrections. Sources support the decisions above; provider behavior must be reconfirmed at the relevant implementation/release gate. Links to future repository paths are not evidence that those files exist.

| ID | Official source | What was verified / limitation |
|---|---|---|
| V01 | [Node release policy](https://nodejs.org/en/about/previous-releases), [release metadata](https://nodejs.org/dist/index.json) | 24 LTS; 24.20.0 and npm 11.19.0; OS artifacts listed. |
| V02 | [Next installation](https://nextjs.org/docs/app/getting-started/installation), [support](https://nextjs.org/support-policy), [ESLint](https://nextjs.org/docs/app/api-reference/config/eslint) | Next 16 LTS, minimum engines, React peer/tooling requirements, separate lint. |
| V03 | [TypeScript 6.0](https://www.typescriptlang.org/docs/handbook/release-notes/typescript-6-0.html), [typescript-eslint support](https://typescript-eslint.io/users/dependency-versions/) | TS 6.0 documented, parser excludes TS 7. Attempt to open TS 7 handbook page failed; not relied on. |
| V04 | [Official npm registry](https://registry.npmjs.org/next/16.3.4) | Queried publisher metadata at `https://registry.npmjs.org/<package>/<version>` for every D02 pin; engines/peers inspected without installing. Reproduce with `npm view <package>@<pin> version engines peerDependencies --json` after approval. |
| V05 | [Vitest](https://vitest.dev/guide/), [Playwright installation](https://playwright.dev/docs/intro), [Docker](https://playwright.dev/docs/docker), [PDF API](https://playwright.dev/docs/api/class-page#page-pdf) | Test engine/OS support, matching browser installs, Chromium PDF API. Production Render sandbox/resource behavior untested. |
| V06 | [Zod](https://zod.dev/), [Temporal polyfill](https://github.com/js-temporal/temporal-polyfill), [Temporal semantics](https://tc39.es/proposal-temporal/docs/zoneddatetime.html), [idb](https://github.com/jakearchibald/idb), [web-push](https://github.com/web-push-libs/web-push) | Selected library purpose and interfaces; no inferred formal LTS guarantees. |
| V07 | [Supabase local development](https://supabase.com/docs/guides/local-development), [CLI](https://supabase.com/docs/guides/local-development/cli/getting-started), [CLI config source](https://raw.githubusercontent.com/supabase/cli/main/apps/cli-go/pkg/config/templates/config.toml) | Docker local stack; template major 17 and local SMTP. Public config defaults can lag source; pinned CLI runtime must be checked. |
| V08 | [Supabase SSR](https://supabase.com/docs/guides/auth/server-side/nextjs), [RLS](https://supabase.com/docs/guides/database/postgres/row-level-security), [database connections](https://supabase.com/docs/guides/database/connecting-to-postgres) | Verified server auth, explicit database grants/RLS, private-cache caution, server connection choices. Use session pooler TLS for long-lived pg clients if direct IPv6 is unavailable. |
| V09 | [Render workers](https://render.com/docs/background-workers), [Docker](https://render.com/docs/docker), [Blueprint](https://render.com/docs/blueprint-spec), [regions](https://render.com/docs/regions), [pricing](https://render.com/pricing) | Continuous worker, container support, current plan identifiers, Singapore. Full pricing table not reliably extractable. |
| V10 | [Supabase regions](https://supabase.com/docs/guides/platform/regions), [pricing](https://supabase.com/pricing), [backups](https://supabase.com/docs/guides/platform/backups) | Singapore, Pro price floor, daily backup availability; full provider erasure commitment unverified. |
| V11 | [Supabase SMTP](https://supabase.com/docs/guides/auth/auth-smtp), [Resend SMTP](https://resend.com/docs/send-with-smtp), [Resend domains](https://resend.com/docs/dashboard/domains/introduction) | Production custom SMTP; verified domain/API key, smtp.resend.com:465 with TLS. No user domain/account inspected. |
| V12 | [Next PWA](https://nextjs.org/docs/app/guides/progressive-web-apps), [WebKit iOS push](https://webkit.org/blog/13878/web-push-for-web-apps-on-ios-and-ipados/), [MDN Push](https://developer.mozilla.org/en-US/docs/Web/API/Push_API) | User-triggered permission, service worker, iOS installed-app requirement; actual delivery/device versions NOT RUN. |
| V13 | [Noto Devanagari](https://github.com/notofonts/devanagari), [WCAG 2.2](https://www.w3.org/TR/WCAG22/) | Source/font licensing direction and accessibility standard; shipped-file rights/rendering proof remains required. |
| V14 | [PDF.js](https://mozilla.github.io/pdf.js/getting_started/), [ESLint support](https://eslint.org/version-support/) | PDF.js stable version/Node engines and current ESLint major; incompatible transitive lint peers avoided. |
| V15 | [S3 access/consistency](https://docs.aws.amazon.com/AmazonS3/latest/userguide/Welcome.html), [conditional writes](https://docs.aws.amazon.com/AmazonS3/latest/userguide/conditional-writes.html), [expiry](https://docs.aws.amazon.com/AmazonS3/latest/userguide/lifecycle-expire-general-considerations.html), [Supabase Auth limits](https://supabase.com/docs/guides/auth/rate-limits) | Independent object ledger/conditional write semantics; lifecycle is asynchronous; provider Auth rate controls are separate from app route counters. Actual configuration NOT RUN. |
